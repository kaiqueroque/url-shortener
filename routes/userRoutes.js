const router = require('express').Router();
const Shortened = require('../models/user');

//Create an user
router.post('/', async (req, res) => {})

//Getting an user data
router.get('/', async (req, res) => {})

module.exports = router;